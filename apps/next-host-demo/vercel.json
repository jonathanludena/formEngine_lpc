{
  "git": {
    "ignoreCommand": "git diff --quiet HEAD^ HEAD ./"
  },
  "buildCommand": "cd ../.. && pnpm install && cd packages/forms && pnpm build && cd ../../apps/next-host-demo && rm -rf .next node_modules/.cache && pnpm build",
  "installCommand": "cd ../.. && pnpm install --force",
  "framework": "nextjs",
  "crons": []
}
