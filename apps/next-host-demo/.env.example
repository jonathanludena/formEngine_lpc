# Local development (SQLite)
DATABASE_URL="file:./prisma/dev.db"

# Production (Turso - uncomment and set for Vercel)
# DATABASE_URL="libsql://[your-db].turso.io"
# TURSO_AUTH_TOKEN="your-turso-auth-token"

# JWT Secret (cambiar en producción)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# API Key para comunicación server-to-server (opcional)
API_KEY="demo-api-key-12345"

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Webhooks externos (Routara)
# Webhook para validación de sesión y obtención de data inicial del claim
WEBHOOK_CLAIM_START_URL="https://your-webhook-url.com/api/claim-start"
# Webhook para enviar el claim completo (datos + archivos) para procesamiento
WEBHOOK_CLAIM_SUBMIT_URL="https://your-webhook-url.com/api/claim-submit"
# API Key para autenticación con los webhooks (header: x-routara-key)
ROUTARA_API_KEY="your-routara-secret-key"
